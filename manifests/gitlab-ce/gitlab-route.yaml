---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: gitlab-route
  namespace: gitlab
spec:
  to:
    kind: Service
    name: gitlab-ce
  port:
    targetPort: https
  tls:
    termination: passthrough