apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: vaultconnection-gcve
  namespace: keycloak
spec:
  skipTLSVerify: true
  address: 'https://vault.hashicorp.local:8200'
